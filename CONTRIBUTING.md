# Contributing

Thanks for helping improve Notes.

## Local Setup

```bash
npm install
npm start
```

Run the type checker before pushing changes:

```bash
npm run typecheck
```

## Development Notes

- Keep UI changes consistent with the existing minimal notes experience.
- Prefer small, focused commits.
- Do not commit local Expo logs, `node_modules`, or generated native folders.
